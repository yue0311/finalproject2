

    function validateForm() {
        var x = document.forms["myform"]["username"].value;
        if (x == "" || x == null) {
          alert("Name must be filled out");
          return false;
        }
      }
    function validateForm(){
            var y = document.forms["myform"]["sex"].value;
            if (y == "" || y == null) {
            alert("性別 必须填写");
            return false;
            }
        }
        function validateForm(){
            var y = document.forms["myform"]["phonenumber"].value;
            if (y == "" || y == null) {
            alert("電話號碼 必须填写");
            return false;
            }
        }
        function validateForm(){
            var y = document.forms["myform"]["selectdate"].value;
            if (y == "" || y == null) {
            alert("出生日期 必须填写");
            return false;
            }
        }
        function validateForm(){
            var y = document.forms["myform"]["sex"].value;
            if (y == "" || y == null) {
            alert("性別 必须填写");
            return false;
            }
        }
        function validateForm(){
            var y = document.forms["myform"]["phonenumber"].value;
            if (y == "" || y == null) {
            alert("電話號碼 必须填写");
            return false;
            }
        }
        function validateForm(){
            var y = document.forms["myform"]["selectdate"].value;
            if (y == "" || y == null) {
            alert("出生日期 必须填写");
            return false;
            }
        }